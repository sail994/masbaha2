name: Compilation iOS Multiplateforme

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Récupérer le code du projet
        uses: actions/checkout@v4

      - name: Configurer Java 17 (Requis pour Gradle KMP)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Configurer Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Donner les permissions d'exécution au script de compilation
        run: |
          if [ -f "gradlew" ]; then chmod +x gradlew; else cd masbaha2 && chmod +x gradlew; fi

      - name: Compiler le Framework pour iPhone
        run: |
          # On se déplace dans le bon dossier si nécessaire
          [ -f "gradlew" ] || cd masbaha2
                   # On lance la compilation globale des frameworks iOS (couvre toutes les cibles configurées)
          ./gradlew :composeApp:assembleReleaseX64 --continue || ./gradlew :composeApp:linkReleaseFrameworkIosX64 || ./gradlew :composeApp:iosX64Binaries

      - name: Archiver le Framework iOS pour le télécharger
        uses: actions/upload-artifact@v4
        with:
          name: Masbaha-iOS-Framework
          path: |
            **/build/bin/iosX64/
            **/build/cocoapods/framework/
